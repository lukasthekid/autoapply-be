# Production Environment Variables Template
# Copy this file to .env on your production server and fill in the values

# Django Settings
SECRET_KEY=your-production-secret-key-here-generate-a-strong-random-key
DEBUG=False
ALLOWED_HOSTS=api.project100x.run.place,5.75.171.23

# CORS Configuration
# Your frontend domain
CORS_ALLOWED_ORIGINS=https://project100x.run.place

# Database Configuration
# Connecting to existing PostgreSQL running on the host
# Use 'host.docker.internal' to connect to host's PostgreSQL from Docker container
# Or use '127.0.0.1' if Docker is in host network mode
DB_HOST=host.docker.internal
DB_PORT=5433
DB_NAME=autoapply
DB_USER=admin
DB_PASSWORD=your-database-password-here

# GitHub Container Registry (for CI/CD)
GITHUB_REPOSITORY_OWNER=your-github-username
GITHUB_REPOSITORY_NAME=autoapply-be

# Optional: Email Configuration (if you plan to add email features)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password

