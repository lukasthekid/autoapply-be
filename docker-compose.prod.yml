# Production Docker Compose override
# Use with: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  web:
    image: ghcr.io/lukasthekid/autoapply-be:latest
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - DEBUG=False
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"


