# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Add your React frontend URL(s) here
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Database Configuration
# For local development: Use localhost (SSH tunnel will forward to server)
# For production: Use the server IP or localhost if on same network
DB_HOST=localhost
DB_PORT=5433
DB_NAME=autoapply
DB_USER=postgres
DB_PASSWORD=your-database-password-here

# SSH Tunnel Configuration (for local development)
# SSH_SERVER=5.75.171.23
# SSH_USER=your-ssh-username
# SSH_KEY_PATH=path/to/your/ssh/key
# SSH_PORT=22

