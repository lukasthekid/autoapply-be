# Django Application connecting to existing PostgreSQL database
services:
  # Django Application
  web:
    build: .
    container_name: autoapply_web
    restart: unless-stopped
    command: >
      sh -c "python manage.py migrate &&
             python manage.py collectstatic --noinput &&
             gunicorn --bind 0.0.0.0:8000 --workers 4 --timeout 120 --access-logfile - --error-logfile - api.wsgi:application"
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/mediafiles
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - autoapply_network
      - postgres_network  # Connect to existing postgres network
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/docs').read()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  static_volume:
  media_volume:

networks:
  autoapply_network:
    driver: bridge
  postgres_network:
    external: true  # Use the existing postgres network

